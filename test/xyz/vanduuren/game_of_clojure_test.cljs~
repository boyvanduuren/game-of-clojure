(ns xyz.vanduuren.game-of-clojure-test
    (:require
     [cljs.test :refer-macros [deftest is testing]]
     [xyz.vanduuren.game-of-clojure :refer [multiply]]))

(deftest multiply-test
  (is (= (* 1 2) (multiply 1 2))))

(deftest multiply-test-2
  (is (= (* 75 10) (multiply 10 75))))
